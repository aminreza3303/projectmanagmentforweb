Phase 1 – Prisma Setup

Completed
- Express + TypeScript structure
- Prisma schema created at `prisma/schema.prisma`
- SQLite connection in `.env.example`
- Swagger endpoint ready at `/docs`
- Migration applied and SQLite DB created

Notes
- On macOS, use an absolute path in DATABASE_URL if migrations fail.
